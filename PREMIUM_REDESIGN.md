# دلّني — Premium Redesign (v2)

إعادة بناء شاملة لموقع وكالة **«دلّني»** للتسويق الرقمي من الصفر (تم حذف `src` القديم
واستبداله بالكامل) بهوية بصرية جديدة: **خرائط منتصف الليل** — Cartographic Wayfinding —
مستوحاة من تخصص الوكالة نفسه (خرائط Google والظهور المحلي).

## 🎨 الهوية البصرية

- **المفهوم:** خريطة ليلية حيّة — شبكة نقاط، مسارات مضيئة متحركة، دبابيس ذهبية، وإحداثيات جغرافية.
- **الألوان:** كحلي عميق (`ink`) + أزرق كهربائي (`brand`) + سماوي (`route`) + بنفسجي (`aurora`) + ذهبي فاخر (`gold`) للتمييز.
- **الخطوط:** Alexandria للعناوين + IBM Plex Sans Arabic للنصوص + Space Grotesk للأرقام والإحداثيات.
- **توقيعات مميزة:** إحداثيات القاهرة في الهيدر، دبوس العلامة بالنجمة الذهبية، بطاقات زجاجية
  بحواف متدرجة (`border-arc`)، ضجيج ناعم، ومسارات SVG متحركة.

## 🧩 التقنيات

React 19 + TypeScript + Vite 7 + Tailwind CSS 4 (CSS-first theme) + Framer Motion 12 +
React Router 7 + lucide-react. بدون مكتبات ثقيلة (لا Three.js) — الخريطة الحية مرسومة
بـ Canvas خفيف GPU-friendly.

## ⚡ الحركة

| التأثير | المكان |
|---|---|
| شبكة خرائط حية بنبضات ضوئية (Canvas) | الهيرو وكل صفحات الأقسام و404 |
| Word-by-word reveal مع Blur | عناوين الهيرو |
| Stagger + Reveal on scroll | كل الأقسام |
| عدّادات متحركة rAF | الإحصائيات |
| Tilt 3D + Glare | بطاقات الخدمات (معطّل للمس/Reduced-motion) |
| Marquee لا نهائي (CSS) | شريط الخدمات |
| Page Transition + Scroll restoration ذكي | التنقل بين الصفحات |
| Accordion بحركة ارتفاع | الأسئلة الشائعة |
| شاشة تحميل أول زيارة | مرة واحدة لكل جلسة |
| كل الحركات تتوقف عند `prefers-reduced-motion` | شامل |

## 📄 الصفحات

- **الرئيسية:** هيرو (عناوين متدرجة + لوحة نتائج عائمة) ← ماركيه ← الخدمات (6 بطاقات Tilt)
  ← إحصائيات ← لماذا نحن ← كيف نعمل ← آراء العملاء ← باقات خرائط Google (1,500 / 2,500 / 4,000 ج.م)
  ← أسئلة شائعة ← CTA.
- **الخدمات:** تفاصيل كل خدمة + باقات بتبويبين (خرائط / حملات إعلانية) + منهجية العمل.
- **من نحن:** القصة، الرؤية والرسالة، القيم، الفريق، الأرقام.
- **المدونة:** 4 مقالات عربية كاملة مع فلاتر وحركة Layout، ومقالات ذات صلة.
- **تواصل معنا:** نموذج يرسل إلى `/api/contact` + بطاقات تواصل + لوحة خرائط.
- **404:** دبوس عائم على خريطة بلا مسار.

## 🤖 التفاعلية

- **دَلّوب:** مساعد ذكي عائم يخاطب `/api/chat` (Gemini مع خطة احتياطية ذكية بدون مفتاح).
- **واتساب عائم** + أزرار واتساب بنص جاهز في كل CTA.

## 🛠 الخادم (`server.ts`)

- `POST /api/chat` — Gemini مع Smart Fallback مبني على قواعد (بدون `GEMINI_API_KEY`).
- `POST /api/contact` — حفظ العملاء محلياً في `data/leads.jsonl` + محاولة Firestore اختيارياً.
- `allowedHosts: true` لاستقبال نطاق المعاينة في بيئة التشغيل.

## ⚡ الأداء

- كل الحركات على `transform`/`opacity` فقط؛ Canvas يتوقف خارج الشاشة وعند إخفاء التبويب.
- `prefers-reduced-motion` يعطّل الحركة بالكامل.
- لا صور ثقيلة — خلفيات CSS/SVG/Canvas (og.jpg للشبكات الاجتماعية فقط).
- البناء كله ملف واحد: **~179KB gzip**.

## ✅ التحقق

- `npm run typecheck` — بدون أخطاء.
- `npm run build` — نجح.
- `npm run dev` — يخدم الموقع على المنفذ 3000 مع كل المسارات.

## 🔧 للتعديل

كل المحتوى (الأرقام، الباقات، آراء العملاء، المقالات، واتساب) في ملف واحد:
`src/lib/constants.ts` — عدّل رقم الواتساب والهاتف قبل الإطلاق.
