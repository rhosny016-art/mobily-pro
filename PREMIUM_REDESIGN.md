# إعادة تصميم موقع دلّني — Premium Redesign

إعادة تصميم شاملة لموقع وكالة "دلّني" التسويقية بطابع فاخر حديث ينافس مواقع الشركات التقنية العالمية، مع الحفاظ الكامل على المحتوى والخدمات وفكرة الموقع.

## 🎨 نظام التصميم الجديد (`src/index.css`)

- لوحة ألوان فاخرة: كحلي عميق + أزرق كهربائي + بنفسجي + سماوي + ذهبي للتمييز.
- 3 خطوط: Cairo للعربية + Montserrat + Space Grotesk للأرقام والعناوين.
- مكوّنات جاهزة: أزرار متوهجة (`btn-primary` / `btn-ghost` / `btn-whatsapp`)، بطاقات زجاجية، حدود متدرجة متحركة (Conic Border)، توهجات، شبكة وخلفية Mesh + ضجيج ناعم.
- سكرولبار مخصص + تحديد نص أنيق + دعم كامل لـ `prefers-reduced-motion`.

## ✨ الأنيميشن (كلها Transform/Opacity على الـ GPU فقط)

| التأثير | المكان |
|---|---|
| Page Transition | انتقال ناعم بين كل الصفحات |
| Text Reveal | كلمة-كلمة مع Blur في الـ Hero |
| Animated Counters | أرقام الإحصائيات تعُدّ عند الظهور |
| Reveal on Scroll | Fade/Slide/Scale في كل الأقسام |
| 3D Tilt + Glare | بطاقات الخدمات (يتفاعل مع الماوس) |
| Parallax | الكرات المضيئة والمشهد في الـ Hero |
| Marquee | شريط الخدمات المتحرك أسفل الـ Hero |
| Animated Borders | إطار CTA الرئيسي والفوتر يدوران بتدرج |
| Floating Elements | شرائح عائمة حول خريطة الخدمات |
| Micro-interactions | أزرار، أيقونات، روابط، بطاقات |
| Loading Animation | شاشة تحميل فاخرة بالشعار والحلقات |

## 🧩 مكوّنات جديدة

- `TiltCard.tsx` — بطاقة ثلاثية الأبعاد مع لمعان متحرك (GPU-light).
- `AnimatedCounter.tsx` — عدّاد أرقام يعمل بـ rAF عند الظهور.
- `AuroraBackground.tsx` — خلفية Aurora قابلة لإعادة الاستخدام (كرات + شبكة + ضجيج).
- `TrustMarquee.tsx` — شريط خدمات متحرك بلا توقف.
- `src/lib/motion.ts` — مكتبة Variants موحّدة لكل الحركات.

## 📄 الصفحات المُعاد تصميمها

- **الرئيسية**: Hero مبهر (كلمات متدرجة، خريطة خدمات 3D، ثقة فورية) → شريط خدمات → خدمات (بطاقات 3D) → إحصائيات (عدّادات) → لماذا نحن → خريطة الشرق الأوسط → آراء العملاء → FAQ أكورديون → CTA.
- **الخدمات**: Hero + شبكة البطاقات + تفاصيل كل خدمة **مع باقات الأسعار (جديدة — كانت البيانات موجودة وغير مستخدمة!)** + CTA.
- **من نحن**: قصة + إحصائيات + رسالة/رؤية/قيم + فريق بحلقات متدرجة + CTA.
- **المدونة**: فلاتر بتأثير Pill متحرك + بطاقات مع AnimatePresence.
- **تفاصيل المقال**: قارئ Markdown فاخر مع CTA متوهج.
- **404**: دبوس خريطة عائم + عنوان متدرج.
- **الناف بار**: زجاجي مع شريط تقدّم القراءة + مؤشر نشط متحرك + قائمة موبايل كاملة.
- **الفوتر**: شريط CTA بإطار متحرك + شبكة أقسام + أزرار سوشيال متوهجة + زر العودة للأعلى.
- **واتساب العائم** + **أزرار الواتساب** + **الصور (Skeleton داكن)** + **نموذج التواصل**.

## 🛠 إصلاحات إضافية

- إصلاح روابط `/#services` لتتنقل بالـ state وتنزل تلقائياً للقسم.
- إصلاح رابط "الرئيسية" في الناف بار (كان يعيد تحميل الصفحة كاملة).
- إصلاح تباين ألوان لوحة التحكم (أبيض على أبيض سابقاً).
- تمكين استهلاك `scrollTo` الذي كان ميتاً.

## ⚡ الأداء

- حركات على `transform`/`opacity` فقط + `will-change` محدود.
- Lazy-loading للخريطة التفاعلية (D3) والصور مع IntersectionObserver.
- `prefers-reduced-motion` يعطّل كل الحركات لمن يطلب ذلك.
- تأثيرات CSS (Marquee/Borders) بدلاً من JavaScript loops.

## ✅ التحقق

- `npm run typecheck` — بدون أخطاء.
- `npm run build` — نجح (gzip ~373KB شاملاً كل شيء).
- `npm run start` — يخدم الموقع على المنفذ الافتراضي.
